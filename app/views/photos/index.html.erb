<% provide(:title, 'Photo') %>
<h1>Gallery</h1>
<%= link_to "Add Photo", new_photo_path, class: "btn btn-large btn-primary" %><br><br>
<% @photos.each do |photo| %>
  <%=link_to image_tag(photo.image.url(:medium)), photo_path(photo) %>
<% end %>
